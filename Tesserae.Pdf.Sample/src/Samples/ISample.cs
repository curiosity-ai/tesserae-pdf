namespace Tesserae.Pdf.Sample
{
    /// <summary>
    /// Marker for a sample page. <see cref="App"/> finds every implementation by reflection, so a new
    /// page is a new class - there is no list to keep in sync.
    /// </summary>
    public interface ISample
    {
    }
}
